# 📄 專題程式檔案簡單說明

## 1. make_dataset.py
- **做什麼**  
  把乾淨的資料切成模型可以用的「輸入 (X) → 輸出 (y)」資料集。  

- **重要參數**  
  - `WINDOW`：用多少筆資料當輸入（例如 12 筆 → 3 小時）。  
  - `HORIZON`：要預測幾步（1 → 下一個 15 分鐘；4 → 下一小時）。  

- **合理原因**  
  這樣切資料符合「用過去預測未來」的概念。  

---

## 2. train_lstm.py
- **做什麼**  
  訓練 LSTM 模型，並存下最好的模型。  

- **重要參數**  
  - `LSTM_UNITS`：模型大小（越大越聰明，但可能過擬合）。  
  - `Dropout`：防止過擬合。  
  - `Learning Rate`：學習速度（太大不穩定，太小太慢）。  

- **合理原因**  
  訓練曲線 (`train_loss.png`) 有下降，代表模型真的學到東西。  

---

## 3. predict.py
- **做什麼**  
  用訓練好的模型在測試資料上做預測，畫圖、算指標。  

- **重要參數**  
  - `模型檔路徑`：要讀哪個模型來預測。  
  - `HORIZON`：要跟 dataset 設定一致。  

- **合理原因**  
  誤差很小 (MAE ≈ 1.5 km/h)，預測線和真實線幾乎重疊 → 模型效果好。  

---

## 4. baseline.py
- **做什麼**  
  提供簡單的對照方法（上一筆數據、平均值）來比較。  

- **重要參數**  
  - `window size`：Moving Average 要平均多少筆。  

- **合理原因**  
  Naive/平均的誤差都比 LSTM 大 → 證明 LSTM 不是亂猜。  

---

# 🎯 總結
- `make_dataset.py` → 整理資料  
- `train_lstm.py` → 訓練模型  
- `predict.py` → 測試模型  
- `baseline.py` → 做比較  

**結果合理，因為**  
- Loss 曲線有下降 → 模型有學到。  
- 驗證/測試誤差小 → 模型穩定。  
- LSTM 比 baseline 好 → 模型是真的有效。  
